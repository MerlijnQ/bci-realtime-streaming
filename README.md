# bci-realtime-streaming
First, run streamer.py in a terminal. You should use python3 -m src.streamer

Then, run the other files in another terminal. For instance, run python3 -m examples.visualize_eeg_fast_plotter

